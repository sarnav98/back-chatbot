#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@infermedica/eslint-config-vue/node_modules/eslint-find-rules/dist/bin/diff.js" "$@"
  ret=$?
else 
  node  "$basedir/../@infermedica/eslint-config-vue/node_modules/eslint-find-rules/dist/bin/diff.js" "$@"
  ret=$?
fi
exit $ret
